{
  "name": "playwright-web-automation-demo",
  "version": "1.0.0",
  "description": "This is a UI automation boilerplate developed using Playwright and TypeScript.",
  "main": "",
  "scripts": {
    "test-dev": "TEST_ENV=dev npx playwright test --headed && npx playwright show-report",
    "test-sit": "TEST_ENV=sit npx playwright test --headed && npx playwright show-report",
    "test-uat": "TEST_ENV=uat npx playwright test --headed && npx playwright show-report",
    "test-prod": "TEST_ENV=prod npx playwright test --headed && npx playwright show-report",
    "headless-test-dev": "TEST_ENV=dev npx playwright test && npx playwright show-report",
    "headless-test-sit": "TEST_ENV=sit npx playwright test && npx playwright show-report",
    "headless-test-uat": "TEST_ENV=uat npx playwright test && npx playwright show-report",
    "headless-test-prod": "TEST_ENV=prod npx playwright test && npx playwright show-report",
    "debug-test-dev": "TEST_ENV=dev npx playwright test --ui"
  },
  "keywords": [
    "playwright",
    "typescript",
    "nodejs",
    "web-automation",
    "test-automation"
  ],
  "author": "Osanda Nimalarathna",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.45.1",
    "@types/node": "^20.14.9"
  },
  "dependencies": {
    "@types/chance": "^1.1.6",
    "chance": "^1.1.12"
  }
}
